{
  "name": "weather-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name geo-watch"
  },
  "dependencies": {
    "maplibre-gl": "^5.24.0",
    "pdfjs-dist": "^6.1.200",
    "vite": "^6.0.0"
  }
}
